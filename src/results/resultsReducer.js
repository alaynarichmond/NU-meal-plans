const reducer = (state = {}, action) => {
    switch(action.type) {
        case 'LOAD_RESULTS':
            return {
                ...state,
                mealPlanPrice: mealPlanData[]
            }
    }
}